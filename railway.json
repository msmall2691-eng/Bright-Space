{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "python scripts/db_bootstrap.py",
        "startCommand": "sh -c 'export UVICORN_WORKERS=${UVICORN_WORKERS:-4}; uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --workers $UVICORN_WORKERS --proxy-headers --forwarded-allow-ips=\"*\"'",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
