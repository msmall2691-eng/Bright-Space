# Development-only dependencies. NOT installed in production builds
# (Dockerfile installs requirements.txt only).
#
# Install locally with:
#   pip install -r requirements-dev.txt

mypy==1.13.0
types-python-dateutil==2.9.0.20240821

# Test runner. httpx (FastAPI's TestClient dependency) is pinned once in
# requirements.txt — it is a runtime dep too — so it is not repeated here;
# a second pin here conflicted with the BB-SEC-17 bump (0.27.2 vs 0.28.1).
pytest==9.1.1
